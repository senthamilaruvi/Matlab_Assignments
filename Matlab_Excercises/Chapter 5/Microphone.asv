% To get input from user and compare with threshold value
% Find th average for values above threshold
t = input('Enter the threshold value:');
n = input('Enter the number of values:');
value = 0;
for i = 1:n
     = input('Enter the voltage:');
    if value > t